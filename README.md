# AI Marketplace Agents

FastAPI server for AI agents managing brand-creator marketplace.

## Setup

```bash
pip install -r requirements.txt
python main.py
```

## Endpoints

- `GET /health` — Health check
- `GET /` — Root

## Deploy

FastAPI on Railway.
